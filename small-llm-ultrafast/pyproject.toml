[project]
name = "small-llm-ultrafast"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["jev-ultrafast"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
jev-ultrafast = { path = "../jev-ultrafast", editable = true }
